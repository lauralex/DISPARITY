# DISPARITY Engine Feature Guide

This document is the practical test map for the current Visual Studio 2022 C++20/DirectX 11 engine.

## Editor

- Press `F1` to show or hide the Dear ImGui editor.
- Drag panels into the main dockspace, or pull them outside the main window to test multi-viewport support.
- Use `Hierarchy` to select the player or a scene object.
- Selected objects render with a bright outline in the main viewport.
- Press `Tab` to release the mouse, then left-click the main viewport to pick objects. Hold `Ctrl` while picking or selecting in `Hierarchy` to multi-select. Picks try GPU object-ID/depth readback for scene objects, the player, and gizmo handles, then fall back to stable-ID CPU OBB tests.
- Use `Copy`, `Paste`, `Duplicate`, and `Delete` in the `Hierarchy` panel or `Edit` menu. Keyboard shortcuts are `Ctrl+C`, `Ctrl+V`, `Ctrl+D`, and `Delete`.
- The `Viewport` panel enables an editor camera. Use `Frame Selection`, `Frame Player`, right-drag look, arrow/Page keys, or right-drag plus `WASD`/`Q`/`E` to inspect the scene without moving the player.
- The viewport preview includes a compact diagnostics HUD over the renderer-owned texture. It shows active camera/render mode, bloom/TAA state, last GPU-picked object/depth/stale age, readback latency/cache state, high-resolution capture tile/resolve state, and small object-ID/depth debug thumbnail swatches. Use `Viewport > Viewport HUD` to enable, pin, or hide individual HUD rows.
- The viewport texture also has an overlaid toolbar. Use `Cam` to toggle editor/game camera, `Dbg` to cycle post debug views, `Cap` to queue the capture workflow, `ID` and `Depth` to toggle object-ID/depth rows, and `HUD` to hide or restore the diagnostics overlay.
- Editor preferences autosave under `Saved/Editor/editor_preferences.json`. They remember viewport HUD rows/pinning, viewport-toolbar visibility, editor-camera enablement, transform precision/pivot/orientation, workspace preset metadata, dock-layout checksum metadata, the Profiler command-history filter, and the active named profile. Use `Viewport > Preference Profiles` to save/load/import/export sanitized profile files under `Saved/Editor/Profiles`, diff the current surface against defaults, or reset the current editor preference surface to defaults.
- Use `Viewport > Workspace Presets` to switch the editor between gameplay, editor, and trailer-capture layouts; the same panel shows capture progress diagnostics for the current preset.
- Use `Inspector` to edit transforms/materials. The `Transform Gizmo` section provides small move, scale, and yaw buttons without adding another third-party dependency, and the viewport draws camera-scaled 3D gizmo handles at the current selection pivot. Choose translate/rotate/scale and world/local space in `Viewport`, then left-drag an axis marker, rotate ring, or translucent translate-plane handle; hold `Shift` while dragging for snapping. Gizmo handles brighten on hover or drag.
- Use `Inspector > Transform Precision` to set the gizmo nudge step and inspect the active pivot/orientation mode used by the transform handles.
- Use `Inspector > Prefab Overrides` on a selected scene object to compare against `Assets/Prefabs/Beacon.dprefab`, apply the current object back to the prefab, or revert object mesh/material/scale while preserving world position and stable ID.
- Use `Ctrl+Z` and `Ctrl+Y`, or the `DISPARITY` menu, to test undo/redo. The profiler shows recent command labels and includes a command-history filter for long editor sessions.
- The `Profiler` panel includes production readiness tables for the forty v25 points, thirty-six v28 diversified points, thirty v29 public-demo points, thirty-six v30 vertical-slice points, thirty v31 diversified roadmap points, sixty v32 roadmap points, fifty v33 playable-demo points, sixty v34 AAA-foundation points, fifty v35 engine-architecture points, sixty v36 mixed engine/game/editor points, and thirty v38 diversified engine/editor/game points, while v39, v40, v41, v42, v43, v44, and v45 roadmap readiness is enforced through runtime reports, schemas, baselines, and tooling. Together these make editor workflow, asset promotion, rendering, runtime sequencer, audio production, publishing, playable-demo coverage, core engine services, typed config, telemetry, editor-panel workspace state, runtime command state, event-route coverage, content-backed production surfaces, live production asset validation, runtime catalog activation, live production catalog bindings, breadth-first verification, and the game source split inspectable while the build is running.

## Engine Architecture

- `DisparityEngine` now has a typed `EventBus` for immediate and queued engine events. Runtime verification checks typed subscribers, wildcard audit subscribers, deterministic flushes, unrouted drop counts, and queue-depth diagnostics.
- `FrameScheduler` defines explicit Input, Simulation, Physics, Animation, Audio, Rendering, and Editor phases. Tasks carry names, priority, enabled state, and diagnostics for registered, executed, skipped, per-phase, and frame counts.
- `SceneQueryWorld` stores stable-ID AABBs and supports sphere overlap, AABB overlap, nearest raycast, layer masks, hit counts, and layer-reject diagnostics. This is the seed for future picking, triggers, AI perception, and controller collision sharing one query surface.
- `AssetStreamingPlan` models priority/pinned streaming requests under request and byte budgets. It records submissions, scheduled/deferred/cancelled requests, dependency edges, pending work, loaded work, and scheduled bytes.
- `AnalyzeRenderGraphBudget` samples the live render graph against pass/resource/barrier/alias budgets and reports budget status into runtime verification and the Profiler.
- `ServiceRegistry` records engine/game/editor subsystem readiness, required-service health, duplicate registration attempts, and initialized service counts.
- `TelemetryStream` gives engine, game, editor, asset, and rendering code one structured place for counters, gauges, event channels, and bounded ring-buffer overflow diagnostics.
- `ConfigVarRegistry` stores typed bool/int/float/string runtime tuning values with dirty tracking, preparing the current editor controls for future console commands and persistent project settings.
- `RuntimeCommandRegistry` centralizes named runtime/editor/game commands with categories, default bindings, enable/disable state, duplicate tracking, execution diagnostics, search, execution history, binding-conflict detection, category summaries, success/failure history splits, bound/unique binding counts, documented command counts, and required-category validation for the next command-palette pass.
- `EditorPanelRegistry` centralizes panel descriptors, deterministic ordering, docking targets, visibility toggles, searchable panels, workspace presets, saved dock-layout descriptors, team-default flags, workspace command bindings, controller navigation hints, toolbar customization slots, migration-ready layout counts, focus target coverage, gamepad navigation readiness, toolbar profile coverage, workspace application diagnostics, and simple navigation state for the live editor panel surface.
- The v37-v45 source split moves stable game batch data into `DisparityGame/Source/DisparityGame/GameFollowupCatalog.*` and `GameProductionCatalog.*`, shared prototype data shapes into `GameRuntimeTypes.h`, helper/math/argument parsing into `GameRuntimeHelpers.*`, v39-v45 roadmap readiness/reporting into `GameRoadmapBatch.*`, v45 production-catalog editor/game bridging into `GameProductionRuntimeCatalog.*`, and module/source-readability metadata into `GameModuleRegistry.*`. `DisparityGame.cpp` remains the orchestration layer, but it no longer carries the extracted catalogs, data model, or current report writers inline.
- `GameEventRouteCatalog` documents objective, encounter, traversal, audio, and failure event routes with telemetry counter names, event channel names, replay flags, breadcrumb labels, selection-link metadata, trace channels, checkpoint/save/chapter replay metadata, accessibility-critical flags, and HUD-visible flags so public-demo gameplay routes are visible outside `DisparityGame.cpp`.
- v42 content-backed production surfaces live under `Assets/Runtime`, `Assets/SceneSchemas`, `Assets/Streaming`, `Assets/Rendering`, `Assets/Editor`, `Assets/Cinematics`, and `Assets/Gameplay`. They seed event-trace channels, scheduler budgets, task graph edges, scene-query layers, streaming budgets, render budget classes, workspace layouts, command palette rows, viewport bookmarks, inspector presets, dock migrations, Shot Director validation, encounter planning, controller feel, objective routes, accessibility settings, save-slot planning, and combat sandbox metadata.
- `ProductionAssetValidator` now validates the v42 production assets as v43 live content: it counts non-comment directives, checks required tokens and activation fields, reports missing-field totals, emits stable hash/version metadata per asset, and rolls those into `v43_validation_points` so docs, baselines, release readiness, and runtime reports cannot drift.
- `ProductionAssetRuntimeCatalog` consumes the v43-validated assets as v44 runtime catalog entries: it parses directive names, entry identifiers, key/value fields, activation entries, domains, actions, required-entry matches, and missing-field totals so future engine/editor/game systems can bind the same content instead of duplicating text checks. v45 adds `ProductionRuntimeBinding` rows, binding diagnostics, ready/active/source/domain counts, field lookup, and a live `GameProductionRuntimeCatalog.*` bridge used by the `Engine Services` panel, runtime reports, and world-space catalog beacons.
- Runtime verification emits `engine_event_bus_deliveries`, `engine_scheduler_tasks`, `engine_scheduler_phases`, `engine_scene_query_hits`, `engine_streaming_scheduled_requests`, `engine_render_graph_budget_checks`, `engine_module_smoke_tests`, `v35_engine_architecture_points`, all fifty `v35_point_*` metrics, `engine_service_registry_services`, `engine_telemetry_events`, `engine_config_vars`, `editor_panel_registry_panels`, `game_module_split_files`, `v36_mixed_batch_points`, all sixty `v36_point_*` metrics, `runtime_command_registry_commands`, `editor_workspace_registry_workspaces`, `game_event_route_catalog_routes`, `v38_diversified_points`, all thirty `v38_point_*` metrics, `runtime_command_history_entries`, `runtime_command_binding_conflicts`, `editor_saved_dock_layouts`, `game_event_route_replayable_routes`, `game_event_route_trace_channels`, `v39_roadmap_points`, all fifteen `v39_point_*` metrics, `runtime_command_history_successes`, `runtime_command_history_failures`, `runtime_command_unique_bindings`, `editor_workspace_migration_ready`, `game_event_route_checkpoint_links`, `game_event_route_hud_visible_routes`, `v40_diversified_points`, all fifteen `v40_point_*` metrics, `v41_breadth_points`, `v41_engine_scheduler_phase_coverage`, `v41_editor_pick_tests`, `v41_game_objective_stages`, `v41_docs_ready`, all twenty `v41_point_*` metrics, `v42_content_points`, `v42_engine_manifest_assets`, `v42_editor_manifest_assets`, `v42_game_manifest_assets`, `v42_verification_manifest_assets`, `v42_docs_ready`, all twenty-four `v42_point_*` metrics, `v43_validation_points`, `v43_engine_live_assets`, `v43_editor_editable_assets`, `v43_game_playable_assets`, `v43_validated_assets`, `v43_activation_bindings`, `v43_missing_fields`, `v43_asset_hash_low`, all twenty-four `v43_point_*` metrics, `v44_catalog_points`, `v44_engine_catalog_assets`, `v44_editor_catalog_assets`, `v44_game_catalog_assets`, `v44_runtime_catalog_assets`, `v44_runtime_catalog_entries`, `v44_runtime_catalog_fields`, `v44_runtime_catalog_activations`, `v44_runtime_catalog_domains`, `v44_runtime_catalog_actions`, `v44_runtime_catalog_missing_fields`, all twenty-four `v44_point_*` metrics, `v45_live_catalog_points`, `v45_runtime_catalog_bindings`, `v45_runtime_catalog_ready_bindings`, `v45_engine_live_bindings`, `v45_editor_live_bindings`, `v45_game_live_bindings`, `v45_catalog_panel_rows`, `v45_catalog_visible_beacons`, `v45_catalog_objective_bindings`, `v45_catalog_encounter_bindings`, `v45_catalog_negative_fixture_tests`, and all twenty-four `v45_point_*` metrics.

## Playable Public Demo

- The default prototype now has a compact playable DISPARITY vertical slice that can be shown without entering editor panels: collect all six glowing rift shards, align three phase anchors, tune two resonance gates, stabilize three phase relays during overcharge windows, dash/vault through traversal blockers, evade simple enemy pressure, retry from checkpoints when stability collapses, and return to the extraction beacon to finish the objective.
- Use `WASD` or an XInput left stick to move, hold `Shift` or gamepad `LB` to sprint, press `Space` or gamepad `A` to dash/vault, and press `P` or gamepad `Start` to pause/resume. The HUD in the top-left shows stage, shard progress, phase-anchor progress, resonance-gate progress, phase-relay progress, stability, focus, checkpoint/retry state, objective distance, extraction readiness, traversal/enemy/animation/menu state, completion state, and the current objective text.
- Press `F10` or use `DISPARITY > Reset Public Demo` to reset shard pickups, phase anchors, resonance gates, phase relays, collision obstacles, enemy state, menu/failure state, checkpoint/retry state, sentinel pressure, extraction state, rift surge, animation state, and objective status while the game is running.
- Public-demo visuals include shard beacons, phase-anchor glyph rings, resonance-gate rings/glyphs, phase-relay rings/glyphs/orbit shards, bridge beams from aligned anchors, tuned gates, and stabilized relays to the rift, relay overcharge warning rings, route markers, traversal barriers, enemy vision rings, chase beams, archetype colors, telegraph rings, a checkpoint marker, low-stability warning rings, extraction gate rings, sentinel hazard columns, stronger rift charge as progress builds, v45 catalog-driven orbiting domain beacons around the rift, and a completion pulse that remains visible in showcase/trailer captures.
- Use the `Demo Director` panel to inspect current stage, objective distance, shard/anchor/gate/relay progress, checkpoint/retry counters, pressure-hit/footstep/combo/collision/traversal/enemy/gamepad/menu/content-audio/animation telemetry, recent gameplay events, and v30/v31/v32/v33/v34 readiness. The v34 section shows enemy archetype/line-of-sight/telegraph/hit-reaction state, controller feel counters, blend-tree counts, accessibility/title-flow readiness, and render/production readiness item counts. It also exposes reset, save-checkpoint, and retry controls for recording/debug passes.
- Runtime verification exercises a deterministic full route, collects all shards, activates all phase anchors, tunes all resonance gates, stabilizes all phase relays, solves collision, triggers traversal, simulates gamepad/menu frames, forces enemy chase/evade/contact and checkpoint retry behavior, exercises enemy archetype line-of-sight/telegraph/hit-reaction behavior, records controller grounded/slope/material/moving-platform/camera-collision samples, records animation blend-tree events, records pressure-hit, footstep cadence, overcharge-window, content audio, animation, failure-presentation, accessibility, and combo-route telemetry, completes extraction, draws the Demo Director/readiness surface, smokes the v35 engine architecture modules, smokes the v36 service/telemetry/config/editor-panel/source-split modules, smokes the v38 command/workspace/event-route modules, smokes the v39 command-history/editor-workspace-route modules, smokes the v40 command binding/editor migration/game route metadata surfaces, smokes the v41 breadth-first engine/editor/game/verification-docs coverage surfaces, and reports `public_demo_tests`, `public_demo_shard_pickups`, `public_demo_completions`, `public_demo_hud_frames`, `public_demo_beacon_draws`, `public_demo_objective_stages`, `public_demo_anchor_activations`, `public_demo_resonance_gates`, `public_demo_phase_relays`, `public_demo_relay_overcharge_windows`, `public_demo_combo_steps`, `public_demo_collision_solves`, `public_demo_traversal_actions`, `public_demo_enemy_chases`, `public_demo_enemy_evades`, `public_demo_enemy_archetypes`, `public_demo_enemy_los_checks`, `public_demo_enemy_telegraphs`, `public_demo_enemy_hit_reactions`, `public_demo_controller_grounded_frames`, `public_demo_controller_slope_samples`, `public_demo_controller_material_samples`, `public_demo_controller_moving_platform_frames`, `public_demo_controller_camera_collision_frames`, `public_demo_animation_clip_events`, `public_demo_animation_blend_samples`, `public_demo_accessibility_toggles`, `rendering_pipeline_readiness_items`, `production_pipeline_readiness_items`, `engine_event_bus_deliveries`, `engine_scheduler_tasks`, `engine_scene_query_hits`, `engine_streaming_scheduled_requests`, `engine_render_graph_budget_checks`, `engine_service_registry_services`, `engine_telemetry_events`, `engine_config_vars`, `editor_panel_registry_panels`, `game_module_split_files`, `runtime_command_registry_commands`, `runtime_command_history_entries`, `runtime_command_history_failures`, `runtime_command_unique_bindings`, `runtime_command_binding_conflicts`, `editor_workspace_registry_workspaces`, `editor_saved_dock_layouts`, `editor_workspace_migration_ready`, `game_event_route_catalog_routes`, `game_event_route_replayable_routes`, `game_event_route_trace_channels`, `game_event_route_checkpoint_links`, `game_event_route_hud_visible_routes`, `v41_engine_event_bus_flushes`, `v41_editor_pick_tests`, `v41_game_audio_animation_tests`, `v41_verification_schema_ready`, `v41_breadth_points`, `public_demo_gamepad_frames`, `public_demo_menu_transitions`, `public_demo_failure_presentations`, `public_demo_content_audio_cues`, `public_demo_animation_state_changes`, `public_demo_pressure_hits`, `public_demo_footstep_events`, `public_demo_retries`, `public_demo_checkpoints`, `public_demo_director_frames`, `public_demo_stability`, `public_demo_focus`, all thirty `v29_point_*` metrics, all thirty-six `v30_point_*` metrics, all thirty `v31_point_*` metrics, all sixty `v32_point_*` metrics, all fifty `v33_point_*` metrics, all sixty `v34_point_*` metrics, all fifty `v35_point_*` metrics, all sixty `v36_point_*` metrics, all thirty `v38_point_*` metrics, all fifteen `v39_point_*` metrics, all fifteen `v40_point_*` metrics, and all twenty `v41_point_*` metrics.

## Showcase Mode

- The first playable scene now includes an animated procedural DISPARITY rift in front of the player: HDR rings, orbiting shards, spires, dynamic colored point lights, and stronger bloom response.
- Press `F7`, use `DISPARITY > Showcase Mode`, or click `Start Showcase` in the renderer panel to hide the editor and enter the cinematic orbit camera.
- Showcase mode temporarily forces tone mapping, bloom, SSAO, anti-aliasing, temporal AA, clustered lighting, and higher color grading values, then restores the previous renderer settings when disabled.
- Press `F7` again to return to the previous editor visibility and gameplay camera state.

## Trailer And Photo Mode

- Press `F8`, use `DISPARITY > Trailer/Photo Mode`, or click `Start Trailer` in the renderer panel to play authored camera shots from `Assets/Cinematics/Showcase.dshot`.
- Trailer mode interpolates position, target, focus, depth-of-field strength, lens dirt, letterbox, easing, renderer pulse, audio cue, bookmark, spline, timeline-lane, renderer-track, audio-track, thumbnail, clip-lane, nested-sequence, hold-time, and shot-role metadata so the same vertical-slice camera move is repeatable for recording and verification.
- Use the `Shot Director` panel to add/edit/capture/reload/save v6 `.dshot` keys while the game is running. Camera keys can use Catmull-Rom or linear interpolation, editable easing curves, renderer/audio timeline tracks, thumbnail tint metadata, generated shot thumbnails, clip lanes, nested sequence labels, hold durations, shot roles, and non-modal preview scrubbing.
- Press `F9` or click `Capture PPM+PNG` in the viewport panel to capture the presented frame, then write `Saved/Captures/DISPARITY_photo_source.ppm`, `Saved/Captures/DISPARITY_photo_source.png`, a 2x `Saved/Captures/DISPARITY_photo_2x.ppm`, and `Saved/Captures/DISPARITY_photo_2x.dcapture.json`. The high-resolution path now flows through graph-owned offscreen capture diagnostics and an async worker; the v3 manifest records the active capture preset, scale, tile count, MSAA samples, tile jitter, tent-like resolve filter, async-compression readiness, and planned EXR output.
- The renderer panel exposes depth of field, lens dirt, vignette, film grain, letterbox, title-safe guides, and presentation pulse controls for public demo capture.
- Automatic generated cue tones are off by default for clean capture with the current WinMM prototype backend. Enable `Audio Mixer > Cinematic cue tones` only when you want to test the temporary generated cues.

## Assets And Prefabs

- Edit `Assets/Scenes/Prototype.dscene`, `Assets/Scripts/Prototype.dscript`, `Assets/Prefabs/Beacon.dprefab`, or the player material files while the game is running.
- Keep `Hot reload assets` enabled in the `Assets` panel and wait about half a second, or press `F5` to force scene/script reload.
- Select a scene object, then use `Apply Selection To Beacon Prefab` to write that object shape/material back to `Assets/Prefabs/Beacon.dprefab`.
- Select a scene object, then use `Save Selection As Runtime Prefab` to write a prefab snapshot under `Saved`.
- Use `Rescan Database` to rebuild the in-editor asset database. It classifies assets, shows dirty cooked state, tracks glTF buffer/image references, material texture-slot references, script prefab references, prefab parent/child references, and reports dependency graph nodes/edges.
- Use `Cook Dirty Assets` to write metadata cache files under `Saved/CookedAssets` through the engine job system.
- Use `Export glTF Materials` to convert loaded glTF material metadata into `.dmat` material assets under `Assets/Materials/Imported`, including base-color, normal, metallic-roughness, emissive, and occlusion texture-slot metadata.
- Import settings use `.dimport` files. `Assets/ImportSettings/Assets/Meshes/SampleTriangle.gltf.dimport` is the sample mesh import settings file.

## glTF

- `Assets/Meshes/SampleTriangle.gltf` is loaded at startup.
- The loader creates renderer meshes for every primitive, tracks primitive material indices, binds base-color textures when present, honors glTF `doubleSided` materials, instantiates mesh nodes into the scene, reads skin joint/weight attributes, stores inverse bind matrices, and parses animation sampler keyframes.
- The `Assets` panel shows mesh, primitive, material, node, skin, and animation counts.
- `glTF animation playback` previews parsed animation channels when an imported asset includes them.

## Rendering

- The renderer remains DirectX 11 and shader-model 5.
- The `Renderer` panel exposes VSync, tone mapping, shadow maps, broader CSM-style shadow coverage, clustered light contribution, bloom, SSAO, anti-aliasing, temporal AA, exposure, shadow strength, bloom threshold/strength, SSAO strength, AA strength, temporal blend, saturation, contrast, depth of field, lens dirt, cinematic overlay, vignette, letterbox, title-safe guides, film grain, presentation pulse, and post debug views.
- The scene uses a directional light, shadow map, and eight point lights, including animated rift lights.
- Post-processing uses the HDR scene texture, temporal history texture, and depth SRV.
- The renderer now owns dedicated editor GPU targets for viewport color, object IDs, and object depth. Scene objects, player meshes, and gizmo handles write IDs/depth to those targets, and the editor queues non-blocking readbacks from a staging ring for GPU-backed picking. The `Viewport` panel presents the dedicated viewport texture through ImGui, overlays live GPU-pick/capture diagnostics, and runtime reports include object-ID readback request/completion/pending/busy/latency diagnostics, hover-cache samples, latency histogram buckets, stale-readback age, and last picked object display.
- Materials can request double-sided rendering. The DX11 path switches those draws to a no-cull rasterizer state and the scene shader flips back-face normals, keeping imported single-surface meshes like the sample triangle visible during rotation.
- The `Profiler` panel shows frame timings, job worker count, renderer draw-call counters, GPU frame timing once DX11 timestamp queries warm up, and compiled render-graph schedule/resource lifetime diagnostics. The render graph view also reports per-pass GPU timings, culled passes, graph callback execution, graph resource binding hits/misses, pending capture requests, graph-owned high-resolution capture target/tile/MSAA/pass diagnostics, read/write transition diagnostics, alias candidates, transient allocation slots, and alias reuse.
- If bloom/SSAO/AA/DOF/lens differences are hard to spot in the final image, use `Renderer > Post debug`: `Bloom` isolates bright bleed, `SSAO mask` shows contact darkening, `AA edges` shows the edge detector, `Depth` visualizes the depth buffer, `DOF` shows the circle-of-confusion mask, and `Lens dirt` isolates the dirt/bloom response.
- Bloom became more visible because the scene shader no longer clamps lighting before the HDR post pass.
- Scene materials now include emissive color and emissive intensity, and the rift uses those channels for glow alongside its animated point lights.

## Audio

- The `Audio Mixer` panel exposes Master/SFX/UI/Music/Ambience buses.
- Use `Low Tone`, `High Tone`, and `Spatial` to test generated bus-routed tones.
- `F2` plays the UI notification tone.
- The mixer shows the active backend, per-bus send values, simple peak meters, active generated voice counts, production-style mixer voice counts, streamed music layers, spatial emitters, attenuation curves, meter updates, and content pulse counts.
- The mixer detects XAudio2 runtime availability and exposes a preference switch. When XAudio2 initializes, generated tones use source voices through a dynamically loaded XAudio2 backend; WinMM remains the fallback path.
- The mixer and runtime report expose audio analysis values: peak, RMS, beat envelope, active voices, and whether generated/streamed content has driven the analysis surface.
- `Cinematic cue tones` is opt-in; showcase/trailer visual beat pulses still run when this is off. Generated cues prefer XAudio2 when available and otherwise reuse a persistent WinMM output handle with a short silent pre-roll so cue playback is not dependent on another app keeping the Windows audio endpoint awake.
- The public demo loads named cue definitions from `Assets/Audio/PublicDemoCues.daudio` for shard, anchor, gate, relay, traversal, enemy, failure, menu, footstep, and extraction beats. Runtime verification checks those content-backed cue paths without requiring a physical audio device.
- Player demo animation states are declared in `Assets/Animation/PublicDemoPlayer.danim`; the current state is reflected in player material tinting and exported through runtime reports for verification.
- Public-demo blend-tree readiness is declared in `Assets/Animation/PublicDemoBlendTree.danimgraph`. It currently stores clip names, transition metadata, animation event markers, and root-motion preview rows so future authored animation import work has a verified content surface.
- Use `Store Snapshot` and `Recall` to test mixer snapshot capture/restore without playing content.
- `AudioSystem::StreamMusic` and `PlayWaveFileAsync` provide WinMM-backed async/looped wave playback hooks for future content, while the public surface now has listener orientation and production-style snapshot/meter APIs for the future XAudio2 backend.

## Asset Cooking

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\CookDisparityAssets.ps1 -Configuration Debug -BinaryPackages
```

The cook writes per-source metadata plus `Saved/CookedAssets/manifest.dcook`. With `-BinaryPackages`, it writes deterministic `.dassetbin` source bundles under `Saved/CookedAssets/Binary`, structured `DSGLBPK2` `.dglbpack` manifests for glTF/glB sources under `Saved/CookedAssets/Optimized`, and package hashes in the manifest. glTF packages include mesh/primitive/material/node/skin/animation counts, accessor and buffer-view metadata, embedded/external buffer hashes, import settings, and dependencies. Runtime verification now loads the optimized package manifest as a cooked runtime resource and validates dependency invalidation coverage.

Review cooked packages with:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\ReviewCookedPackages.ps1
```

## Packaging

Run:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\PackageDisparity.ps1 -Configuration Release -IncludeSymbols -CreateArchive
```

The packaged build is written to `dist/DISPARITY-Release`. Packages include `package_manifest.json`; passing `-IncludeSymbols` copies PDBs under `Symbols`, and `-CreateArchive` writes a zip artifact under `dist`.

Symbol indexing and installer payload generation are separate production checks:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\IndexDisparitySymbols.ps1 -PackagePath .\dist\DISPARITY-Release
powershell -ExecutionPolicy Bypass -File .\Tools\PublishDisparitySymbols.ps1 -PackagePath .\dist\DISPARITY-Release
powershell -ExecutionPolicy Bypass -File .\Tools\CreateDisparityInstaller.ps1 -PackagePath .\dist\DISPARITY-Release -CreateArchive
powershell -ExecutionPolicy Bypass -File .\Tools\CreateDisparityBootstrapper.ps1 -PackagePath .\dist\DISPARITY-Release
```

These write `Symbols/symbols_manifest.json`, a symbol-server publish plan, `dist/SymbolServer/symbol_publish_manifest.json`, `dist/Installer/DISPARITY-SetupManifest.json`, `dist/Installer/DISPARITY-BootstrapperPlan.json`, `dist/Installer/DISPARITY-InstallerBootstrapper.cmd`, and an installer payload zip for later replacement with a real bootstrapper executable.

Release readiness review checks the assembled package, installer/bootstrapper output, symbols, OBS profile, runtime schema manifest, the v25/v28 production batch manifests, the v29 public-demo manifest, the v30 vertical-slice manifest, the v31 diversified roadmap manifest, the v32 sixty-point roadmap manifest, the v33 playable-demo manifest, the v34 AAA-foundation manifest, the v35 engine-architecture manifest, the v36 mixed-batch manifest, the v38 diversified batch manifest, the v39 roadmap batch manifest, the v40 diversified batch manifest, the v41 breadth batch manifest, the v42 production-surface manifest, the v43 live-validation manifest, the v44 runtime-catalog manifest, and the v45 live-catalog manifest:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\ReviewReleaseReadiness.ps1
```

The review writes `Saved/Release/release_readiness_manifest.json` and is part of the full verification gate.

Crash upload staging is local for now:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\CollectCrashReports.ps1 -CreateArchive
powershell -ExecutionPolicy Bypass -File .\Tools\UploadCrashReports.ps1 -DryRun
```

The scripts scan `Saved/CrashLogs`, write `Saved/CrashUploads/crash_upload_manifest.json`, can bundle staged reports, can dry-run the eventual transport step, and retry authenticated uploads with backoff when an endpoint is provided.

Trailer launch preset generation is separate from packaging:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\LaunchTrailerCapture.ps1 -Configuration Release -Packaged -NoLaunch
powershell -ExecutionPolicy Bypass -File .\Tools\GenerateObsSceneProfile.ps1
```

The scripts write `Saved/Trailer/trailer_launch_preset.json` and `Saved/Trailer/OBS/DISPARITY-Trailer-Scene.json` with executable, working directory, capture output, recording hotkey metadata, OBS scene/profile metadata, and recording approval metadata. Omit `-NoLaunch` to start the selected build.

Interactive CI planning is generated by:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\GenerateInteractiveCiPlan.ps1
```

The plan lands in `Saved/CI/interactive_ci_plan.json` and describes the interactive GPU runner, packaged smoke gate, artifacts, runtime suites, and trailer/OBS capture expectations.

## Verification

Use the full local gate before calling the repository healthy:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\VerifyDisparity.ps1
```

The verification script runs `git diff --check`, a Dear ImGui literal ID conflict check, a game source split check, warning-free Debug and Release builds, MSVC static analysis, all shader entry-point compiles, asset cook manifest/binary package generation, optimized `.dglbpack` package review, crash upload manifest/upload dry runs, baseline review, v25/v28/v29/v30/v31/v32/v33/v34/v35/v36/v38/v39/v40/v41/v42/v43/v44/v45 production manifest review, baseline approval manifest generation with Git signature metadata, signed baseline update approval intent, interactive CI/trailer/OBS plan generation, Debug window smoke, every runtime verification suite, Release packaging with symbols/archive, symbol indexing and symbol publishing, installer payload/bootstrapper generation, release readiness review, packaged window smoke, every packaged runtime verification suite, and a performance-history summary against committed baselines.

Targeted checks are still useful while iterating:

```powershell
powershell -ExecutionPolicy Bypass -File .\Tools\SmokeTestDisparity.ps1 -Configuration Debug -ExerciseWindow
powershell -ExecutionPolicy Bypass -File .\Tools\TestImGuiIds.ps1
powershell -ExecutionPolicy Bypass -File .\Tools\RuntimeVerifyDisparity.ps1 -Configuration Debug -Frames 90
.\bin\x64\Debug\DisparityGame.exe --verify-runtime --verify-frames=90
```

Runtime verification writes `Saved/Verification/runtime_verify.txt`, captures `Saved/Verification/runtime_capture.ppm`, runs deterministic input playback, validates capture dimensions/luminance/checksum/nonblack pixels, checks CPU/GPU frame budgets and per-pass render-graph budgets, validates render-graph dispatch order, graph callback execution, graph resource binding, transition barriers, transient allocation counts, alias reuse, async object-ID readback ring diagnostics, hover-cache samples, latency histogram buckets, viewport overlay rows, viewport HUD row toggles/debug thumbnails, transform precision state, editor preference save/load/profile/import/export/diff round trips, workspace preset state, viewport toolbar actions, high-resolution resolve and capture-preset metadata, and editor viewport/object-ID/depth target readiness, confirms the imported glTF triangle is double-sided so it does not vanish when rotated, cycles all seven post-debug views, exercises showcase mode and trailer/photo mode with deterministic rift timing, validates graph-owned high-resolution capture diagnostics plus the async 2x output/manifest workflow, validates rift VFX draw counts plus depth-fade/GPU-simulation/motion-vector/temporal-reprojection/profile/emitter/GPU-buffer counters, validates beat-pulse counts, validates editor pick/gizmo pick coverage, runs gizmo transform constraint checks, validates async text IO, material texture-slot persistence, prefab variant/nested-prefab metadata, cooked package loading/GPU-promotion metadata plus dependency preview/reload rollback/streaming/texture-binding/skinning/retargeting readiness, Shot Director timeline tracks/thumbnails/preview scrubbing/v6 sequencing metadata plus v28 sequencer readiness, animation blending/skinning palette state, XAudio2 backend state, production audio counters, audio analysis, audio meter calibration, audio snapshot capture/restore, release-readiness coverage, deterministic public-demo shard collection/phase-anchor/resonance-gate/phase-relay/collision/traversal/enemy-archetype/controller-polish/blend-tree/gamepad/menu/failure/audio-animation/retry/extraction completion, v35 engine module coverage, v36 service/telemetry/config/editor-panel/source-split coverage, v38 runtime-command/editor-workspace/event-route coverage, v39 command-history/editor-workspace-route coverage, v40 command-binding/editor-workspace-migration/game-route metadata coverage, v41 breadth-first engine/editor/game/verification-docs coverage, v42 content-backed production-surface coverage, v43 live production validation coverage, v44 runtime catalog activation coverage, v45 live production catalog binding/editor/demo coverage, all forty v25 production followup points from `Assets/Verification/V25ProductionBatch.dfollowups`, all thirty-six v28 diversified followup points from `Assets/Verification/V28DiversifiedBatch.dfollowups`, all thirty v29 public-demo points from `Assets/Verification/V29PublicDemo.dfollowups`, all thirty-six v30 vertical-slice points from `Assets/Verification/V30VerticalSlice.dfollowups`, all thirty v31 diversified roadmap points from `Assets/Verification/V31DiversifiedBatch.dfollowups`, all sixty v32 roadmap points from `Assets/Verification/V32RoadmapBatch.dfollowups`, all fifty v33 playable-demo points from `Assets/Verification/V33PlayableDemoBatch.dfollowups`, all sixty v34 AAA-foundation points from `Assets/Verification/V34AAAFoundationBatch.dfollowups`, all fifty v35 engine-architecture points from `Assets/Verification/V35EngineArchitectureBatch.dfollowups`, all sixty v36 mixed-batch points from `Assets/Verification/V36MixedBatch.dfollowups`, all thirty v38 diversified points from `Assets/Verification/V38DiversifiedBatch.dfollowups`, all fifteen v39 roadmap points from `Assets/Verification/V39RoadmapBatch.dfollowups`, all fifteen v40 diversified points from `Assets/Verification/V40DiversifiedBatch.dfollowups`, all twenty v41 breadth points from `Assets/Verification/V41BreadthBatch.dfollowups`, all twenty-four v42 production-surface points from `Assets/Verification/V42ProductionSurface.dfollowups`, all twenty-four v43 live-validation points from `Assets/Verification/V43LiveProductionValidation.dfollowups`, all twenty-four v44 runtime-catalog points from `Assets/Verification/V44RuntimeCatalogActivation.dfollowups`, and all twenty-four v45 live-catalog points from `Assets/Verification/V45LiveProductionCatalog.dfollowups`, downsamples the frame into a thumbnail, compares it against a suite-specific golden PPM, writes a golden diff thumbnail, and exits non-zero if an invariant fails. `RuntimeVerifyDisparity.ps1` asserts the report metrics listed in `Assets/Verification/RuntimeReportSchema.dschema`, so missing viewport/capture/v24/v25/v26/v27/v28/v29/v30/v31/v32/v33/v34/v35/v36/v38/v39/v40/v41/v42/v43/v44/v45 production diagnostics fail fast without duplicating schema lists in multiple scripts. Budget defaults can be overridden through `RuntimeVerifyDisparity.ps1` or direct flags such as `--verify-cpu-budget-ms=120`, `--verify-gpu-budget-ms=50`, and `--verify-pass-budget-ms=60`.

Runtime replay and baseline expectations are assetized:

- `Assets/Verification/RuntimeSuites.dverify` defines named runtime suites. The current suites are `Prototype`, `CameraSweep`, `EditorPrecision`, `PostDebug`, `AssetReload`, and `GizmoDrag`.
- `Assets/Verification/Prototype.dreplay` defines frame ranges, movement vectors, and camera drift for deterministic playback.
- `Assets/Verification/CameraSweep.dreplay` adds a camera-heavy deterministic playback path.
- `Assets/Verification/EditorPrecision.dreplay` adds editor-picking coverage for stable-ID object picks and gizmo handle picks.
- `Assets/Verification/PostDebug.dreplay`, `AssetReload.dreplay`, and `GizmoDrag.dreplay` add scenario labels and replay paths for production-style verification coverage.
- `Assets/Verification/*Baseline.dverify` files define expected capture dimensions, average luminance tolerance, nonblack pixel ratio, minimum replay path distance, editor pick counts, gizmo pick counts, post-debug counts, showcase/trailer frame counts, high-resolution capture counts, rift VFX draw counts, beat-pulse counts, async IO/material/prefab/shot/spline/audio-analysis/XAudio2/VFX-system/GPU-VFX/animation-skinning/GPU-pick-histogram/Shot-Director-thumbnail/graph-high-res/cooked-package/nested-prefab/audio-production/viewport-overlay/high-resolution-resolve/viewport-HUD/transform-precision/command-history/schema-manifest/shot-sequencer/VFX-profile/cooked-GPU/dependency-invalidation/audio-calibration/release-readiness/editor-preference/viewport-toolbar/profile/capture-preset/VFX-emitter/cooked-dependency-preview/editor-workflow/asset-pipeline-promotion/rendering-advanced/runtime-sequencer/audio-production-feature/production-publishing/public-demo test counts, the `min_v25_production_points=40`, `min_v28_diversified_points=36`, `min_v29_public_demo_points=30`, `min_v30_vertical_slice_points=36`, `min_v31_diversified_points=30`, `min_v32_roadmap_points=60`, `min_v33_playable_demo_points=50`, `min_v34_aaa_foundation_points=60`, `min_v35_engine_architecture_points=50`, `min_v36_mixed_batch_points=60`, `min_v38_diversified_points=30`, `min_v39_roadmap_points=15`, `min_v40_diversified_points=15`, `min_v41_breadth_points=20`, `min_v42_content_points=24`, `min_v43_validation_points=24`, `min_v44_catalog_points=24`, and `min_v45_live_catalog_points=24` gates, phase-relay/overcharge/combo-route/collision/traversal/enemy/gamepad/menu/failure/content-audio/animation/enemy-archetype/controller-polish/blend-tree/accessibility requirements, render-graph callback/barrier/allocation/alias/resource-binding requirements, editor GPU target requirements, v36 service/telemetry/config/editor-panel/source-split requirements, v38 runtime-command/editor-workspace/event-route requirements, v39 command-history/workspace-route requirements, v40 command-binding/workspace-migration/event-route requirements, v41 breadth-first engine/editor/game/verification-docs requirements, v42 production-surface content requirements, v43 live production validation requirements, v44 runtime catalog requirements, v45 live production catalog requirements, performance budgets, and golden thumbnail tolerances.
- `Assets/Verification/RuntimeReportSchema.dschema` lists required runtime report metrics for wrapper-side schema checks.
- `Assets/Verification/V25ProductionBatch.dfollowups` lists the forty stable v25 followup points that runtime verification must promote to report metrics.
- `Assets/Verification/V28DiversifiedBatch.dfollowups` lists the thirty-six stable v28 followup points across editor workflow, asset pipeline, rendering, runtime sequencer, audio, and publishing.
- `Assets/Verification/V29PublicDemo.dfollowups` lists the thirty public-demo points across gameplay, visuals, HUD, audio feedback, capture, verification, and production hygiene.
- `Assets/Verification/V30VerticalSlice.dfollowups` lists the thirty-six stable v30 vertical-slice points across gameplay, visuals, HUD, editor, backend telemetry, rendering/capture, verification, and production.
- `Assets/Verification/V31DiversifiedBatch.dfollowups` lists the thirty stable v31 points across Editor, Asset Pipeline, Rendering, Runtime, Audio, and Production.
- `Assets/Verification/V32RoadmapBatch.dfollowups` lists the sixty stable v32 points across Editor, Asset Pipeline, Rendering, Runtime, Audio, and Production.
- `Assets/Verification/V33PlayableDemoBatch.dfollowups` lists the fifty stable v33 points across CollisionTraversal, EnemyAI, GamepadMenu, FailurePresentation, and AudioAnimation.
- `Assets/Verification/V34AAAFoundationBatch.dfollowups` lists the sixty stable v34 points across EncounterAI, Controller, Animation, EditorUX, Rendering, and Production.
- `Assets/Verification/V35EngineArchitectureBatch.dfollowups` lists the fifty stable v35 points across Core/EventBus, Runtime/Scheduler, Scene/Queries, Assets/Streaming, and Rendering/Production.
- `Assets/Verification/V36MixedBatch.dfollowups` lists the sixty stable v36 points across Engine, Editor, Game, Assets, Rendering, Runtime, Verification, and Production.
- `Assets/Verification/V38DiversifiedBatch.dfollowups` lists the thirty stable v38 points across Engine, Editor, and Game.
- `Assets/Verification/V39RoadmapBatch.dfollowups` lists the fifteen stable v39 points across Engine, Editor, and Game.
- `Assets/Verification/V40DiversifiedBatch.dfollowups` lists the fifteen stable v40 points across Engine, Editor, and Game.
- `Assets/Verification/V41BreadthBatch.dfollowups` lists the twenty stable v41 points across Engine, Editor, Game, and Verification/Docs.
- `Assets/Verification/V42ProductionSurface.dfollowups` lists the twenty-four stable v42 points across Engine, Editor, Game, and Verification/Docs.
- `Assets/Verification/V43LiveProductionValidation.dfollowups` lists the twenty-four stable v43 points that structurally validate the v42 production assets and their activation metadata.
- `Assets/Verification/V44RuntimeCatalogActivation.dfollowups` lists the twenty-four stable v44 points that consume the v43-validated production assets into parsed runtime/editor/game catalog summaries.
- `Assets/Verification/V45LiveProductionCatalog.dfollowups` lists the twenty-four stable v45 points that bind runtime catalog entries into engine diagnostics, editor tables, visible public-demo beacons, and verification/docs gates.
- `Assets/Verification/V45MalformedCatalog.dprod` is a negative fixture used to prove malformed live catalog content is rejected before it becomes a runtime binding.
- `Assets/Verification/Goldens/*.ppm` stores suite-specific 64x36 golden thumbnails.
- `Assets/Verification/GoldenProfiles/*.dgoldenprofile` stores per-machine or per-adapter golden tolerance defaults. The runtime verification wrapper detects the primary display adapter and uses a matching profile when present, otherwise it falls back to the default profile.
- `Assets/Verification/PerformanceBaselines.dperf` stores committed CPU/GPU/pass thresholds per suite for trend comparison.
- `Tools/CompareCaptureDisparity.ps1` creates or compares capture thumbnails against goldens and writes diff thumbnails.
- `Tools/VerifyGameSourceSplit.ps1` keeps `DisparityGame.cpp` below the configured line budget, requires the focused game split files in `DisparityGame.vcxproj`, and rejects extracted catalog/type blocks returning to the root file.
- `Tools/SummarizePerformanceHistory.ps1` groups recent local runs by suite/executable and reports CPU/GPU deltas plus editor/gizmo/showcase/trailer/high-res/VFX/beat/viewport-overlay/high-resolve/schema/release/editor-preference/viewport-toolbar/profile/capture-preset/VFX-emitter/cooked-dependency/v28-category/v29-public-demo/v30-vertical-slice/v31-diversified/v32-roadmap/v33-playable-demo/v34-AAA-foundation/v35-engine-architecture/v36-mixed-batch/v38-diversified/v39-roadmap/v40-diversified/v41-breadth/v42-production-surface/v43-live-validation/v44-runtime-catalog/v45-live-catalog scenario counters, then compares them against committed performance baselines when available. Regression checks compare both the previous run and the recent median so isolated max-frame spikes remain visible without immediately failing the gate.
- `Tools/ReviewVerificationBaselines.ps1` checks runtime baselines for required capture, graph, editor target, golden, v24-v45 coverage fields, v28 category gates, v29 public-demo gates, v30 vertical-slice gates, v31 diversified gates, v32 relay/roadmap gates, v33 playable-demo gates, v34 AAA-foundation gates, v35 engine-architecture gates, v36 mixed-batch gates, v38 diversified gates, v39 roadmap gates, v40 diversified gates, v41 breadth gates, v42 production-surface gates, v43 live-validation gates, v44 runtime-catalog gates, and v45 live-catalog gates before running the performance summary.
- `Tools/ReviewProductionBatch.ps1` checks the v25 followup manifest, v28 diversified manifest, v29 public-demo manifest, v30 vertical-slice manifest, v31 diversified manifest, v32 roadmap manifest, v33 playable-demo manifest, v34 AAA-foundation manifest, v35 engine-architecture manifest, v36 mixed-batch manifest, v38 diversified manifest, v39 roadmap manifest, v40 diversified manifest, v41 breadth manifest, v42 production-surface manifest, v43 live-production-validation manifest, v44 runtime-catalog manifest, and v45 live-catalog manifest for expected counts, unique point names, domains, and descriptions, then writes review JSON under `Saved/Verification`.
- Cook, package, crash, symbol, and baseline approval tools use a local SHA-256 helper that falls back to .NET streams when `Get-FileHash` is not present in the active PowerShell host.
- `Tools/ApproveVerificationBaseline.ps1` writes `Saved/Verification/baseline_approval.json` with hashes for runtime baselines, performance baselines, golden profiles, and golden thumbnails plus Git signature status/signing-key metadata. Use `-RequireSignedHead` when approving a golden/performance update locally.
- `Tools/ApproveVerificationUpdate.ps1` writes `Saved/Verification/baseline_update_approval.json` with explicit approver intent, Git signature status, and verification file hashes for baseline/golden changes.
- `Saved/Verification/performance_history.csv` is appended by `RuntimeVerifyDisparity.ps1` so repeated local runs leave a trend trail without committing generated data.

Crashes write a small report to `Saved/CrashLogs`. GitHub Actions runs the static side of `Tools/VerifyDisparity.ps1` on push and pull request; opt-in `workflow_dispatch` inputs can create package artifacts, run packaged smoke, upload artifacts, and run the runtime gate when a runner has an interactive desktop.
